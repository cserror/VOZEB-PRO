# VOZEB-PRO 二开工作区规则

本文件是本 Fork 的项目级指令入口。Codex 在仓库根目录发现本文件时，会优先使用它，而不会自动合并同目录的上游 `AGENTS.md`。上游文件保留为详细规则参考；开始具体任务后，按本文件的路由要求定向读取相关章节。

## 开始任务

1. 确认当前工作区是本仓库，不混用其他项目或旧 checkout 的结论。
2. 读取 `PROJECT_CONTEXT.md`，核对当前阶段、Git、运行环境、已验证范围和下一道门。
3. 执行 `git status --short --branch`，保护已有改动；准备写共享规则或当前状态入口时，再读取磁盘最新版本和相关 diff。
4. 只读取当前任务需要的代码、测试和文档 owner。不要把 README、聊天、旧记录或上游文件中的目标描述直接当成当前运行事实。
5. 涉及具体业务域时，按“详细规则路由”读取上游 `AGENTS.md` 的对应章节；规则与当前代码、测试或已确认目标冲突时，先说明冲突，不得静默选边。

## 项目与许可边界

- 本项目是 `csyqlz/VOZEB-PRO` 的自运营二开 Fork，不做 OEM。
- `origin` 指向 `cserror/VOZEB-PRO`，用于保存我们的版本；`upstream` 指向官方仓库，用于检查和同步上游更新。
- 当前仍按 AGPL-3.0 公开使用和修改。取得明确商业授权之前，不得把未来闭源计划写成当前许可，不得移除许可证、版权或必要声明，也不得把仓库改为闭源分发基线。
- 站点名称、Logo、图标、SEO、页脚、模型渠道和运营参数优先使用后台现有配置；只有后台无法覆盖且用户确认需要代码级品牌调整时才修改源码。
- `main` 是稳定、可部署分支。范围小且验证完整的修改可以进入 `main`；较大、跨模块或未完成功能使用 `feature/*`，修复可使用 `fix/*`。

## 修改原则

- 先读现有代码、测试和配置，再做最小必要修改；沿用 Next.js、React、TypeScript、Ant Design、Tailwind、Zustand 和现有服务端结构。
- 不顺手重构、格式化、重命名或清理无关文件，不为未确认的未来需求增加抽象、兼容分支或基础设施。
- Route Handler 负责 HTTP 边界，业务规则放在 `web/src/lib/server/`，数据库访问沿用 `web/src/lib/server/database/` 的 Repository、参数化查询和事务边界。
- 业务数据由服务端和 PostgreSQL 管理；浏览器只保存瞬时 UI 状态，不得重新引入浏览器业务数据持久化兜底。
- 用户输入、外部内容、模型响应、媒体和导出内容必须按使用场景校验、清理或转义。
- 计费、幂等、异步生成、媒体归属与删除、权限、支付、备份和数据库变更属于高风险业务域。修改前必须读取对应实现、测试、上游详细规则和现有契约；不能根据函数名或单个页面推断完整行为。

## 本地环境与命令

- 根目录只是仓库入口；Web 依赖和命令在 `web/`，文档站依赖和命令在 `docs/`。不要在根目录误生成锁文件或依赖目录。
- 官方源码开发基线是 Node.js 22、pnpm 10+、PostgreSQL 16；当前本机 Node.js 版本和本地端口覆盖以 `PROJECT_CONTEXT.md` 为准。服务器 Docker 继续使用项目 Dockerfile 的 Node.js 版本。
- 本地真实配置只写入被 Git 忽略的 `.env.local` 或 `.env`，不得输出、提交或复制真实密码、Token、API Key、私钥和完整环境变量。
- 安装 Web 依赖：`corepack pnpm --dir web install --frozen-lockfile`
- Web 类型检查：`corepack pnpm --dir web typecheck`
- Web 局部测试：`corepack pnpm --dir web test -- <相关测试文件>`
- Web 全量测试：`corepack pnpm --dir web test`
- Web lint：`corepack pnpm --dir web lint`
- Web 生产构建：`corepack pnpm --dir web build`
- 官方开发启动：`cd web && corepack pnpm dev`，默认占用 `3000`；当前机器若端口冲突，使用 `PROJECT_CONTEXT.md` 记录的本地启动方式，不停止其他项目来抢占端口。
- 文档站命令只在文档站任务中从 `docs/` 运行，不把文档站误认为主应用。

## 详细规则路由

上游 `AGENTS.md` 是详细约束来源，但内容可能随上游更新，也可能包含历史性规则。只按当前任务定向读取，并用代码、测试和当前文档复核：

- 后端、数据库、认证、权限、支付、计费、模型渠道、异步任务：读取“后端规范”和“项目注意事项”。
- `/create`、管理后台、媒体展示、响应式、主题和普通页面交互：读取“前端规范”。
- Canvas 节点、连线、布局、弹窗和 Canvas Agent：读取“画布 UI 规范”及相关前端规则。
- README、功能说明、todo、待测试记录、数据库或接口文档：优先执行本文件“文档治理”；上游“文档规范”只用于识别现有路径和历史约束，不得重新引入与本文件冲突的强制流转。
- 版本号、CHANGELOG 和 tag：读取“发版本流程”，但 Git 提交、tag、push 仍以本文件和用户当前授权为准。
- 任意代码修改的验收：读取“Mandatory Testing”，再按本文件“验证与完成”确定本次实际范围。

如果某条详细规则描述的是具体像素、历史 Bug 或旧产品决策，必须先确认它仍与当前代码、测试和目标一致。已由自动化测试稳定保护的细节优先交给测试，不继续复制到本文件。

## 文档治理

每类事实只保留一个规范 owner，其他文档只做摘要、导航或历史记录，不复制同一份现行结论：

- `AGENTS.override.md`：本 Fork 当前生效的长期项目规则和文档路由。
- `AGENTS.md`：上游详细规则参考，保持便于同步；不在其中追加我们的工作区规则。
- `PROJECT_CONTEXT.md`：当前事实入口，只记录当前阶段、来源、运行状态、已验证范围、重要未知项和下一道门，不记录历史流水。
- `README.md`：面向人的项目介绍、核心能力、快速开始和文档入口；避免继续堆叠详细功能、实施状态和运维台账。
- `docs/index.md`：面向 AI 和维护者的文档导航，只说明“什么事实去哪里找”，不复制正文。
- `docs/content/docs/overview/features.mdx`：当前已经实现且有代码、测试或运行证据支持的功能说明。
- `docs/content/docs/progress/todo.mdx`：已经确认要做但尚未实现的事项；探索想法和未确认需求不直接进入。
- `docs/content/docs/progress/pending-test.mdx`：代码已经实现，但仍确实依赖人工、外部供应商、真实付费调用或生产环境验收的事项。
- `docs/content/docs/overview/production-readiness.mdx`：生产准备门和未关闭生产缺口的唯一 owner；其他文档只能链接或摘要。
- `docs/content/docs/backend/api-response.mdx` 与 `docs/content/docs/backend/backend-database.mdx`：接口响应和数据库结构的契约 owner。
- `CHANGELOG.md` 与 Git 历史：版本级历史和已发布变更记录，不承担 current truth、任务清单或逐条验收明细。

状态流转按证据决定，不按固定文档流水线决定：

- 已完成且自动化测试、构建或真实浏览器验收足以证明的变更，可以直接更新对应 owner 和 `CHANGELOG.md` 的版本级摘要，不必先进入 `pending-test.mdx`。
- 只有代码已完成但证据必须来自人工、供应商、真实付费调用或生产环境时，才进入 `pending-test.mdx`；取得证据后移出并更新对应 owner。
- 未实现事项只在已经确认范围时进入 `todo.mdx`；取消、替代或不再适用时直接删除或归档，不要求机械搬运。
- 生产缺口只在 `production-readiness.mdx` 维护完整状态；`todo.mdx`、`pending-test.mdx` 和 `CHANGELOG.md` 不逐条复制。

不再执行以下上游历史做法：每个任务都强制更新 todo/pending、所有改动都必须经过完整三段流转、把用户手工确认作为唯一验收方式、在多个文档复制生产缺口、禁止在状态或证据记录中使用必要日期。日期只在能说明核验时间、版本边界或外部时效时记录，不写无意义的到期日期。

现有 `todo.mdx` 和 `pending-test.mdx` 内容较大，视为需要分批核对的上游遗留文档债务，不能直接当作全部仍有效的 current truth，也不得在一次普通任务中整体清空。后续应逐项按代码、测试、运行证据和生产门分类，再删除重复或失效内容。

不因为一次小改创建新的产品、架构、流程或进度文档。确实没有现有 owner 且需要长期维护时，先说明文档受众、生命周期和替代关系。

## Git 与上游同步

- 不重复执行 `git init`；本仓库已经有完整历史、`origin` 和 `upstream`。
- 完成修改闭环后检查 `git status`、目标 diff 和 `git diff --check`。
- 未经用户明确要求，不自动 commit、tag、push、创建 PR、合并或改写历史。
- 默认提交格式为 `<type>: <中文说明>`，例如 `docs: 建立二开项目规则与当前状态入口`。
- 同步上游时先只读执行 fetch、比较提交和检查许可证/迁移/配置变化；不得把 `origin` 与 `upstream` 混为一谈，也不得未经审查直接 merge、rebase 或覆盖我们的修改。
- 生产部署必须锚定 `main` 的明确 commit 或 release tag，不部署来源不明或包含未说明本地改动的目录快照。

## 验证与完成

- 文档或配置修改至少执行格式/引用检查、`git diff --check` 和 Git 状态核对。
- 核心逻辑、接口、权限、持久化、状态机、计费、解析和可复现 Bug 必须有针对性自动化回归入口。
- 可见页面或交互发生变化后，真实启动项目并用浏览器验证受影响流程；类型检查或构建不能代替页面验收。
- 测试范围按风险确定。局部检查只能证明局部行为；未运行的完整测试、真实模型调用、FFmpeg、支付、备份恢复和生产验证必须明确标为未验证。
- 完成前清理由本轮创建且无正式用途的临时文件，重新检查文档、代码、测试、运行证据和 Git 状态是否一致。

## 上线边界

- 采用既定流程：本地开发验证 -> 明确 Git commit -> 上线交接 -> 运维审查 -> 用户确认生产操作 -> 从该 commit 制作脱敏源码包 -> 上传服务器版本目录 -> 服务器构建 Docker 镜像 -> 备份/迁移判断 -> 切换容器 -> 生产验收。
- 项目工作区负责代码、测试、部署材料和上线交接，不直接执行生产部署。服务器、域名、证书、数据库迁移、备份恢复和容器切换由运维工作区审查并在用户确认后执行。
- 本地数据库、管理员、密钥、上传媒体和后台运营配置不会自动同步到服务器；生产首次部署使用独立环境配置、数据库和管理员。

## 当前状态维护

- 项目阶段、版本、远程仓库、端口、数据库、运行证据、部署状态、长期未知项或下一道门变化时，更新 `PROJECT_CONTEXT.md`。
- 小文案、小样式、临时调试和普通实现细节不更新当前状态入口。
- 修改本文件或 `PROJECT_CONTEXT.md` 后，新任务才会按新规则重新发现指令；不得假定当前已打开的旧任务已经热加载。
