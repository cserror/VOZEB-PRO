name: Docs Docker image (retired)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - run: |
          echo 'Documentation image publishing is outside the approved migration scope.'
          exit 1
